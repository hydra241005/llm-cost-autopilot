services:
  - type: web
    name: api
    env: python
    plan: free
    startCommand: uvicorn autopilot.api.main:app --host 0.0.0.0 --port $PORT
  - type: web
    name: dashboard
    env: python
    plan: free
    startCommand: streamlit run dashboard/app.py --server.port $PORT --server.headless true
